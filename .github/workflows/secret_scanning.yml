---

name: Scan and Detects hardcoded secrets

on:
  workflow_call:

jobs:
  secret-scan:
    runs-on: ubuntu-latest

    steps:
     - name: Checkout the repository
       uses: actions/checkout@v4

     - name: Run Gitleaks
       uses: gitleaks/gitleaks-action@v2
